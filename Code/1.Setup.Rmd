
This is a file for 

1. Setting file paths for code, raw data imports, manipulated (processed) data, and outputs.
2. Also listed are all of the dependency libraries required to build this package. 
3. Importing all the functions required for the package


```{r setup, echo=FALSE, warning=FALSE, message=FALSE}

knitr::opts_chunk$set(
  error=TRUE,
  echo=TRUE,
  cache=FALSE,
  tidy=TRUE,     # display code as typed
  fig.width=12, 
  fig.height=8,
  fig.cap=FALSE,
 # size='large',
  fig.crop=TRUE,
  fig.pos='H',
  eval = TRUE,
  tidy.opts=list(width.cutoff=60))
  eval.after = "fig.cap"
options(knitr.table.format = "latex")

# Prints errors in red
knitr::knit_hooks$set(error= function (x, options) {
  x<-xfun:::split_lines(x)
  sprintf('\\color{red}  %s \\newline \\color{black}', x) })


# Load libraries required for the whole AppSAS package

if (!require("pacman")) install.packages("pacman")
options(gdistance.debug = TRUE, scipen=999)
pacman::p_load(rstudioapi
               ,rmarkdown
               ,knitr
               ,terra
               ,sf
               ,raster
               ,gdistance
               ,rgeos
               ,ggplot2
               ,devtools
               ,stringr
               ,kableExtra
               ,tinytex
               ,patchwork
               ,stringr
               ,svDialogs
               ,ggspatial
               ,ggsn
               ,formatR
               ,pander
               ,tictoc
              )



# Set working directory to be the filepath of this file
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))


# Load R functions needed for reading and processing data
files.sources = list.files(pattern="fn.")
sapply(files.sources, source)
```